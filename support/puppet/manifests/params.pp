#
# Tweak these variables to adjust your development environment:
#
class params {
    $filepath = '/vagrant/support/puppet/modules'

    # Hostname of the virtualbox (should point to 33.33.33.10)
    $host = 'amsterdamphp.dev'

    # Database credentials
    $dbuser = 'amsterdamphp'
    $dbpass = 'amsterdamphp'
    $dbname = 'amsterdamphp'
}
